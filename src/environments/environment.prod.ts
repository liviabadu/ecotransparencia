/** Build de produção (`ng build` ou `--configuration=production`). */
export const environment = {
  production: true,
  /** Ver `environment.ts` — preencha quando a API informar a última atualização da base. */
  dashboardDataLastRefreshLabel: '' as string,
};
